
	
<% samples = stage.versions.first.samples %>

<%# samples = Sample.where(id: stage.versions.ids) %>


<div class="container-fluid">
	<div class="row well">
		<% samples.each do |sample| %>
			<div class="col-md-3">
				<%= render_sample_with_link(sample) %>
				<%= render_note_indicator(sample) %>
			</div>
		<% end %>
	</div>
</div>
