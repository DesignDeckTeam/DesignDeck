<div class="col-md-12">

<h2 class="text-center">用户需求确认</h2>
<br><br>
<table class="table table-striped">
  <thead>
    <tr>
      <th>订单号</th>
      <th>项目主题</th>
      <th>需求描述</th>
      <th>类型</th>
      <th>用户名</th>
      <th>创建时间</th>
      <th>用户选中的图样</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><%= @order.id %></td>
      <td><%= @order.title %></td>
      <td><%= @order.description %></td>
      <td><%= @order.type_preference %></td>
      <td><%= @order.user.email %></td>
      <% if @versions.image_from_customer.present? %>
      <%= image_tag(@versions.image_from_customer.thumb.url, class: "thumbnail") %>
      <%else %>
        <td><%= image_tag("http://placehold.it/200x200&text=No Pic", class: "thumbnail") %></td>
      <% end %>
    </tr>
  </tbody>
</table>

<br><br><br><br><br>

<h3> 上传设计好的图片给用户 </h3>
<br><br>

<%= simple_form_for [:designer, @order] do |f| %>
<div class="group">
  <div class="row">

    <div class="col-md-4">
        <% if @versions.first.present? %>
        <%= image_tag(@versions.first.image_from_designer.medium.url, class: "thumbnail") %>
      <% else %>
        <%= image_tag("http://placehold.it/200x200&text=No Pic", class: "thumbnail") %>
      <% end %>
      <%= f.input "image1_from_designer", as: :file %>
    </div>
  </div>
</div>


  <%= f.submit "Submit", data: { confirm: "你确定要提交当前图片吗？" } %>
<% end %>
