<h2 class="well text-center">
	正在等待客户选择方案
</h2>


<% if @current_stage.versions.present? %>

  <div class="container-fluid btn-group">
    <% @current_stage.versions.each_with_index do |version,i| %>

    	<div class="row">
    		<%= link_to("第 #{i+1} 稿", designer_order_stage_version_path(@order, @current_stage, version), :class => "btn btn-default") %>
    		<br><br>
    		<% version.samples.each do |sample| %>
    			<div class="col-md-4">
    				<%= render_sample(sample) %>
    			</div>   				
    		<% end %>
    	</div>
    <% end %>
  </div>
<% end %>

<hr>
