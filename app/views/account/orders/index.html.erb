<div class="container">
  <h3>您的所有订单</h3>
  <hr>
  <div class="row">
    <div class="col-md-3">
      <!-- <a href='<%= new_account_order_path %>'> -->
      <%= link_to new_account_order_path do %>
        <button class="btn btn-primary btn-block">

        <div class="project-new">
          <h4><span class="glyphicon glyphicon-plus"></span></h4>
           添加设计委托
         </div>
       </button>
       </a>
       <% end %>
     </div>
    <% @orders.each do |order| %>
    <%= link_to account_order_path(order) do %>
    <div class="col-md-3 blue-btn">
      <div class="thumbnail">
        <div class="button-height text-center">
          <h4><%= truncate(order.title, length:10) %></h4>
          <hr>
          <br>
          <br>
          <br>
          <br>
          <br>
          <br>
          <br>
          <br>
          <p><%= order.created_at.strftime("%Y年%m月%d日 %H:%M") %></p>
        </div>
      </div>
    </div>
    <% end %>
    <% end %>
    </div>
  </div>
</div>
